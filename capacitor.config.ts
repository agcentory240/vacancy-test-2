import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.vacancy.app2',
  appName: 'BookList',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
